/**
 * 
 * @param {Server} app - is http.Server from current instance that addons was load 
 * @param {object} cfg - is config of this addons
 */
function handler(app, cfg) {
    
}

module.exports = handler;